import React from 'react';

const GradientOverlay = () => {
  return (
    <div className="absolute inset-0 bg-gradient-to-r from-transparent via-[#937127]/20 to-transparent 
                   opacity-50 animate-gradient-x" />
  );
};

export default GradientOverlay;